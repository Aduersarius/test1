#include <iostream>
#include <sstream>
using namespace std;
int main()
{
	char a[80];
    cin.getline(a, 80);
	int len = strlen(a);
	for (int i = 0; i < len; i++) {
		if ( isspace(a[i]) ) 
		{ 
			cout << "%20";
		}
		else 
		{
			cout << a[i];
		}
	}
	return 0;
}
